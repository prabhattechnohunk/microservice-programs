#application name and property file in git should be same, 
#also application.properties should be renamed to bootstrap.properties if u want to read configuration using config server

spring.application.name=limits-service

spring.cloud.config.uri=http://localhost:8888